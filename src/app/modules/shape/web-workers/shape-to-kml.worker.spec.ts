import { ShapeToKml.Worker } from './shape-to-kml.worker';

describe('ShapeToKml.Worker', () => {
  it('should create an instance', () => {
    expect(new ShapeToKml.Worker()).toBeTruthy();
  });
});
